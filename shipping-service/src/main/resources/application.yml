spring:
  application:
    name: shipping-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8084

logging:
  level:
    com.saga.orchestrator.shipping: DEBUG

# Load testing configuration - 20% failure rate for shipping
shipping:
  failure:
    rate: 0.20
  slow:
    response:
      rate: 0.05
      delay:
        ms: 600000

