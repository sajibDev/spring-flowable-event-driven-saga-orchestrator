spring:
  application:
    name: order-service
    
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081

logging:
  level:
    com.saga.orchestrator.order: DEBUG
